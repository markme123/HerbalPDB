args=commandArgs(TRUE)
inputFile <- args[1]
library(svglite)
#svg("CAZY.svg")
#pdf("CAZY.pdf")
#a<-dev.cur()
#png("CAZY.png")
#dev.control("enable")
library("RColorBrewer")
mycolors <- brewer.pal(5, "Set2")
cazy <- read.table(file=inputFile, head =T , sep ="\t")
percentage <- round((cazy$Num/sum(cazy$Num))*100, 2)
percentage <- paste(percentage, "%", sep = "")
label <- paste(cazy$Class, percentage, sep = ": ")
svg("CAZY.svg")
pie(cazy$Num, labels = label, col = mycolors, border = "white", main = "Carbohydrate-Active enZYmes")
dev.off()
pdf("CAZY.pdf")
pie(cazy$Num, labels = label, col = mycolors, border = "white", main = "Carbohydrate-Active enZYmes")
dev.off()
png("CAZY.png")
pie(cazy$Num, labels = label, col = mycolors, border = "white", main = "Carbohydrate-Active enZYmes")
dev.off()
#dev.copy(which=a)
#dev.off()
#dev.off()
